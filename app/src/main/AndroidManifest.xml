<?xml version="1.0" encoding="utf-8"?>
<!--
  FMZlinkR
  Derived from ShizuCallRecorder.
  Copyright (C) 2026-present kitsumed (Med)
  Copyright (C) 2026 fumizo07
  Licensed under GNU GPL v3 or later with applicable Section 7 terms.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="moe.shizuku.manager.permission.API_V23" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <queries>
        <package android:name="jp.co.rakuten.mobile.rcs" />
    </queries>

    <application
        android:name="com.fumizo07.fmzlinkr.FmzApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="FMZlinkR"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FMZlinkR">

        <activity
            android:name="com.fumizo07.fmzlinkr.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.FMZlinkR">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name="com.fumizo07.fmzlinkr.services.recording.RakutenLinkRecordingService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Rakuten Link VoIP recording readiness and user-requested recording via Shizuku UserService." />
        </service>

        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:enabled="true"
            android:exported="true"
            android:multiprocess="false"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />
    </application>

</manifest>
